// Define an array of numbers
const numbers = [1, 2, 3, 4, 5];

// Print the first number in the array
console.log(numbers[0]);

// Create a new object with key-value pairs
const data = {
    name: "John",
    age: 25,
    address: "123 Main St"
};

// Log the properties of the object
console.log(data.name, data.age, data.address);

// Calculate the square root of the number 9
const squareRoot = Math.sqrt(9);

// Check if the value is NaN (Not a Number)
if (isNaN(0)) {
    console.log("The value is not NaN");
} else {
    console.log("The value is NaN");
}
